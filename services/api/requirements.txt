fastapi>=0.115
uvicorn[standard]>=0.34
psycopg[binary]>=3.2
python-multipart>=0.0.9
PyJWT>=2.9

# Pinned to match the root requirements.txt exactly (not just "a compatible
# version") — app/parsing.py imports grp_calculator.py directly, and it's
# the same module the Streamlit app runs, so a pandas/openpyxl version drift
# here could make the two runtimes parse the same upload differently.
pandas==3.0.5
openpyxl==3.1.5
xlrd==2.0.2
